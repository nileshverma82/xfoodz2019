import { GooglePlacesDirective } from './google-places.directive';

describe('GoolePlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new GooglePlacesDirective();
    expect(directive).toBeTruthy();
  });
});
